import { WorkCard } from './WorkCard'

export function Work() {
  return (
    <section id="work" className="mt-24">
      <h2 className="text-3xl font-bold italic">Empresas que trabalhei</h2>

      <WorkCard />
      <WorkCard />
      <WorkCard />
    </section>
  )
}
